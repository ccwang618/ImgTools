# 角色

你是一位精通網頁設計、前端工程與瀏覽器 Web API 的工程師。

# 任務

請建立一個單頁 `.html` 工具，用來透過 Web Cam 即時解碼 QR Code 與常見條碼。此工具必須可以直接以本機 HTML 或 localhost 開啟使用。

# 產出檔案

請輸出：

- `webcam_barcode_qrcode.html`
- `output_content.md`

# 語言與文字要求

- 所有介面文字、狀態訊息、錯誤提示、按鈕、表單標籤、輔助說明都必須使用繁體中文。
- 所有原始碼註解都必須使用中文。
- 中文使用全形標點符號。
- 英文與數字兩側保留半形空白。
- 不要在頁面中出現英文操作說明。

# 核心功能

請實作以下功能：

- 使用 Web Cam 即時掃描與解碼。
- 支援 QR Code。
- 支援常見一維與二維條碼，例如 Code 128、Code 39、EAN-13、EAN-8、UPC-A、UPC-E、ITF、PDF417、Data Matrix。
- 優先使用瀏覽器原生 `BarcodeDetector`。
- 若瀏覽器不支援 `BarcodeDetector`，請加入 `ZXing` 作為相容解碼器。
- 若所有解碼器都不可用，仍需允許使用者嘗試開啟 Web Cam 預覽，並以中文清楚提示目前無法解碼。
- 可選擇攝影機，預設優先使用手機或裝置的後鏡頭。
- 可停止掃描。
- 可上傳圖片進行條碼或 QR Code 解碼。
- 顯示最新解碼結果。
- 顯示解碼格式。
- 提供複製解碼結果按鈕。
- 保留最近掃描紀錄。
- 可清除掃描紀錄。
- 顯示目前瀏覽器支援或相容解碼器可處理的格式。

# 狀態與錯誤處理

請至少處理以下狀態，且訊息都要是繁體中文：

- 尚未開啟攝影機。
- 已就緒，請開啟攝影機。
- 正在等待攝影機權限。
- 掃描中，請對準條碼。
- 已偵測到內容。
- 已停止掃描。
- 已停止攝影機預覽。
- 無法開啟攝影機，請確認已允許權限，並檢查攝影機是否被其他程式占用。
- 此瀏覽器不支援攝影機存取。
- 此瀏覽器可預覽攝影機，但不支援解碼。
- 已啟用相容解碼器。
- 圖片中未偵測到條碼。
- 圖片解碼失敗。
- 結果已複製。
- 掃描紀錄已清除。

# 介面設計要求

請設計成現代、乾淨、實用的工具介面，不要做成行銷 landing page。

畫面結構：

- 頂部標題：`Web Cam 條碼與 QR Code 解碼器`
- 副標說明：提示使用者開啟攝影機並將 QR Code 或條碼放入掃描框。
- 左側或主要區域：攝影機預覽畫面。
- 預覽畫面上要有掃描框與掃描線動畫。
- 預覽畫面未開啟時，顯示中文提示。
- 工具列包含：
  - 開啟攝影機
  - 停止掃描
  - 選擇攝影機
  - 選擇圖片檔
- 側邊區域包含：
  - 最新解碼結果
  - 複製按鈕
  - 掃描紀錄
  - 清除按鈕
  - 支援格式列表

視覺要求：

- 使用響應式版面，桌機為左右分欄，手機改為單欄。
- 卡片圓角不超過 `8px`。
- 不要使用大面積紫色、深藍、米色或單一色系。
- 文字不能溢出容器。
- 按鈕與表單在手機寬度下需可完整顯示。
- 掃描畫面需有固定比例，避免載入或狀態變更時版面跳動。

# 技術要求

- 使用純 HTML、CSS、JavaScript。
- 不需要建置工具。
- 盡量不依賴框架。
- 可使用 `ZXing` 的 UMD CDN 作為 `BarcodeDetector` 不支援時的相容解碼器。
- 攝影機存取使用 `navigator.mediaDevices.getUserMedia()`。
- 攝影機清單使用 `navigator.mediaDevices.enumerateDevices()`。
- 頁面關閉或停止掃描時，必須停止所有 media tracks。
- 使用 `requestAnimationFrame()` 做原生 `BarcodeDetector` 的連續掃描。
- 畫面上可用 `canvas` 繪製偵測框。
- 使用 `navigator.clipboard.writeText()` 複製結果，並提供基本備援。
- 避免因解碼器不支援而停用攝影機預覽。

# `output_content.md` 內容要求

請簡短說明：

- 已建立的檔案。
- 工具功能。
- 使用方式。
- 注意事項。

# 驗收標準

完成後，頁面應符合以下條件：

- 開啟頁面時不應有 JavaScript 語法錯誤。
- 介面全程使用繁體中文。
- 若 `BarcodeDetector` 可用，能使用原生解碼器掃描。
- 若 `BarcodeDetector` 不可用，能嘗試使用 `ZXing` 相容解碼器。
- 若攝影機或解碼器不可用，能以中文清楚提示原因。
- 使用者按下「開啟攝影機」時，會進入攝影機權限流程，而不是停在靜態提示畫面。
- 最新結果、複製、掃描紀錄、清除紀錄、圖片解碼都要可以操作。
