# 生成圖片浮水印批次工具的提示詞

請製作一個單檔靜態網頁工具，檔名為 `index.html`，用原生 `HTML`、`CSS`、`JavaScript` 完成，不依賴後端伺服器或外部套件。工具用途是在多張圖片上批次加入文字 watermark，所有處理都在使用者本機瀏覽器完成，不上傳圖片。

## 基本需求

建立一個「圖片浮水印批次工具」，可讓使用者選擇圖片檔或輸入資料夾，設定文字 watermark 樣式，然後輸出成指定圖片格式。

介面語言使用繁體中文。

## 輸入功能

1. 使用者可選擇多個圖片檔。
2. 使用者可選擇整個輸入資料夾，並讀取資料夾中的圖片。
3. 可選擇輸入檔案類型：
   - 全部圖片
   - `PNG`
   - `JPG / JPEG`
   - `WebP`
4. 顯示已選擇的圖片數量。
5. 顯示已選圖片清單，包含檔名與檔案大小。
6. 若選擇資料夾，圖片依檔名排序。

## Watermark 設定功能

提供以下設定項目：

1. Watermark 文字內容。
2. 字型選擇，例如：
   - 微軟正黑體
   - 新細明體
   - `Arial`
   - `Times New Roman`
   - `Courier New`
3. 字體大小，單位為 `px`。
4. 文字顏色。
5. 透明度，使用百分比設定。
6. 字串之間的間隔，使用滑桿設定，單位為 `px`。
7. 字串傾斜度，使用滑桿設定，單位為度數。

## Watermark 繪製規則

1. 使用 `canvas` 讀取圖片並繪製 watermark。
2. Watermark 文字需重複鋪滿整張圖片。
3. Watermark 需支援旋轉傾斜。
4. Watermark 需支援透明度。
5. 同一行重複出現的 watermark 文字不可頭尾重疊。
6. 同一行文字間距應使用「實際文字寬度 ＋ 使用者設定的字串間隔」計算。
7. 實際文字寬度需使用 `canvas` 的 `ctx.measureText(text).width` 取得。
8. 若 watermark 文字很長，也要確保同一行相鄰字串不重疊。

## 輸出功能

1. 使用者可選擇輸出資料夾。
2. 使用者可選擇輸出檔案類型：
   - `PNG`
   - `JPG`
   - `WebP`
3. `JPG` 與 `WebP` 可設定輸出品質，範圍為 `1` 到 `100`。
4. 輸出檔名需在原檔名後加上 `_watermark`。
   - 範例：`photo.jpg` 輸出為 `photo_watermark.png`
5. 若瀏覽器支援 File System Access API，使用者可選擇輸出資料夾並直接寫入檔案。
6. 若瀏覽器不支援選擇輸出資料夾，需自動改用逐張下載模式。
7. 批次輸出時需顯示進度。
8. 批次輸出時需顯示處理紀錄，包含成功與失敗訊息。

## 預覽功能

1. 選擇圖片後，顯示第一張圖片的 watermark 預覽。
2. 修改 watermark 文字、字型、大小、顏色、透明度、間隔、傾斜度時，預覽需即時更新。
3. 預覽區需顯示圖片檔名、尺寸與檔案大小。
4. 未選擇圖片時，預覽區顯示提示文字。

## 介面設計

1. 頁面第一畫面即為可操作工具，不做 landing page。
2. 使用左右欄配置：
   - 左側為輸入、輸出、watermark 設定。
   - 右側為圖片預覽與輸出進度。
3. 版面需支援桌面與手機螢幕。
4. 控制項需清楚易用，包含：
   - 按鈕
   - 下拉選單
   - 顏色選擇器
   - 數字輸入框
   - 滑桿
   - 分段切換按鈕
5. 不要使用過度裝飾的視覺效果。
6. 文字不可超出容器或互相重疊。
7. 頁面需顯示「本機處理，不上傳圖片」的提示。

## 技術要求

1. 使用單一 `index.html` 完成全部功能。
2. CSS 與 JavaScript 可直接內嵌在 `index.html`。
3. 不使用外部 CDN。
4. 不使用後端。
5. 不使用第三方套件。
6. 使用 `FileReader` 或 `URL.createObjectURL()` 載入圖片。
7. 使用 `canvas.toBlob()` 產出輸出圖片。
8. 使用 `showDirectoryPicker()` 支援輸出資料夾，並保留瀏覽器不支援時的下載 fallback。
9. 程式需避免阻塞操作，批次處理時需逐張更新進度。
10. JavaScript 需避免 console error。

## 驗證項目

完成後請確認：

1. 頁面可直接在瀏覽器中開啟。
2. JavaScript 語法正確。
3. 選擇圖片後可以看到預覽。
4. 修改 watermark 設定時預覽會更新。
5. 同一行的 watermark 文字不會頭尾重疊。
6. 可以批次輸出圖片。
7. 可輸出 `PNG`、`JPG`、`WebP`。
8. 不支援輸出資料夾的瀏覽器仍可逐張下載。

